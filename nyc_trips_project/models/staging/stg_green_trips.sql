Select 
    cast(vendor_id as string) as vendor_id,
    pickup_datetime,
    dropoff_datetime,
    passenger_count,
    trip_distance,
    cast(rate_code as string) as rate_code,
    store_and_fwd_flag,
    cast(payment_type as string) as payment_type,
    fare_amount,
    extra,
    mta_tax,
    tip_amount,
    tolls_amount,
    imp_surcharge,
    total_amount,
    pickup_location_id, 
    dropoff_location_id,
    'green' as taxi_type,
    cast(ehail_fee as numeric) as ehail_fee,
    cast(trip_type as string) as trip_type_green,
    cast(distance_between_service as numeric) as distance_between_service,
    cast(time_between_service as int64) as time_between_service
from {{ source('tlc', 'tlc_green_trips_2022')}}